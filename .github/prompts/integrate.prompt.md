---
description: "Land an approved change: verify the Done gate passed, fold in the specifications, archive the plan, merge, and close the loop. Lifecycle stage 4."
argument-hint: "[work-item-id]"
---

Carry out the `integrate` skill for: $ARGUMENTS

If no work item identifier appears above, ask for one before starting.

Follow that skill exactly, including its human confirmation points and its guardrails. The Done gate is a hard precondition for the merge.
