# Family Context Spec

Family owns household identity, members, and access.

It is the upstream source for member identity used by Calendar and Tasks.
Areas are family-scoped supporting labels and do not create a separate bounded context.
