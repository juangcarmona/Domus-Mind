# Create Task

Create a task for a family.

The command stores the task title, optional description, schedule, color, and assignee.
An area may be attached later as optional classification data in read models or UI flows.
