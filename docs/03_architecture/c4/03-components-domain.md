# C4 — Domain Components

Primary domain components:

- Family
- Calendar
- Tasks

Areas are a lightweight supporting concept used in projections and read models.
They are not a standalone domain module or aggregate root.
