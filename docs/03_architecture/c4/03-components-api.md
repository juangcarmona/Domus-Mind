# C4 — API Components

The API exposes controllers for family, calendar, tasks, routines, auth, setup, and supporting read models.

Areas are served as a lightweight family-facing read model endpoint.
No dedicated ownership or transfer API exists.
