# DomusMind — Calendar Context

Calendar owns plans/events, schedules, reminders, and participants.

Calendar read models may expose optional area classification to improve grouping and filtering.
Areas do not change calendar rules or event lifecycle behavior.
